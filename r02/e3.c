#include <stdio.h>

int main(int argc, char **argv) 
{
    // FIXME: Mondify the value of answer so that the printf will print 1069547520.
    float answer = 0;

    // DO NOT modify following lines.
    unsigned* unsigned_ptr = (unsigned*)(&answer);
    printf("%u\n", *unsigned_ptr);
}
