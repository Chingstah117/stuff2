#include <stdio.h>

int main(int argc, char **argv) 
{
    // FIXME: Mondify the value of answer so that the printf will print 39661568.
    unsigned answer = 0;

    // DO NOT modify following lines.
    answer = (answer >> 5) << 6;
    printf("%u\n", answer);
}
